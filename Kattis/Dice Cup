/**
 *    Author: lulu (Tasmir_)
 *    Created: 22-January-2024  15:29:48
**/
#include<bits/stdc++.h>
using namespace std;

#define optimize() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);

#define ll long long

//====================================

void solve(){
	ll a, b; cin>>a >>b;
	if(a>b) swap(a,b);
	for(ll i=a; i<=b; ++i){
		cout<<i+1 <<'\n';
	}
}


int main(){
	optimize();

	ll t=1;
	// cin>>t;
	while(t--)
		solve();
}
